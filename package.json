{
  "name": "personalcapitalsearchapp",
  "version": "1.0.0",
  "description": "PersonalCapital Search SPA ",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "open http://localhost:8000 && ./node_modules/.bin/http-server -a localhost -p 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tanmay Shrivastava",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0"
  }
}
